##Spring Configuration
server:
  port: 8081
spring:
  application:
    name: balance-movements
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:

urlbase: https://balance-movements.getsandbox.com:443

balance:
  url: /balance

transaction:
  url: /movement